# Codex Skills

This repository stores reusable Codex skill packages.

Each first-level directory is one standalone skill:

- `ppt-com-scene-renderer`: editable PowerPoint diagram generation through PowerPoint COM.

Keep project-specific outputs, generated decks, previews, and temporary assets in separate project repositories or workspaces.
